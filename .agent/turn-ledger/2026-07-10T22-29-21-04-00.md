# Turn Ledger: MyCozyIsland

Timestamp: `2026-07-10T22-29-21-04-00`

## Goal

Advance the oldest eligible Publish repository's internal architecture documentation without changing runtime code.

## Completed

- compared the full accessible Publish inventory against central ledger state
- excluded `TheCavalryOfRome`
- selected `MyCozyIsland` as the oldest eligible repository
- reviewed route startup, performance budget, render controls, debug surface, and tests
- refreshed root `.agent` summaries
- added a timestamped tracker and system audits
- preserved lifecycle authority as implementation gate one
- documented adaptive-quality transaction/recovery authority as gate two
- prepared central ledger and change-log synchronization
- created no branch or pull request

## Main finding

Recovery to performance level 0 does not restore the renderer's startup pixel ratio because the route only calls `renderer.setPixelRatio()` for levels greater than 0. Declared budget state and applied renderer state can diverge without diagnostic proof.

## Runtime impact

```txt
runtime files changed: no
render tuning changed: no
quality thresholds changed: no
dependencies changed: no
deployment changed: no
```
