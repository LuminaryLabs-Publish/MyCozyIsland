# Turn Ledger: Agriculture Cutover Recovery Audit

Timestamp: `2026-07-12T12-50-46-04-00`

## Goal

Document the official Agriculture DSK cutover and define the missing recovery authority across Inventory, Agriculture, transaction ledgers, ECS events, save migration and visible-frame proof.

## Completed

- Compared all ten accessible Publish repositories and excluded `TheCavalryOfRome`.
- Selected only `LuminaryLabs-Publish/MyCozyIsland` because commit `6b642344d2875f76eef1184111793ff0e206109f` introduced a new runtime Agriculture cutover after the prior audit boundary.
- Confirmed `cozy-farming-domain-kit` is no longer installed.
- Confirmed official `n:production:agriculture` is imported from pinned NexusEngine-Kits source.
- Identified all active domains, 13 installed engine kits, 50 cataloged world/render/host kits and one additional composition kit.
- Audited tropical configuration, Inventory settlement, Agriculture planning and commit, rollback, recovery, save-v2 capture, save-v1 migration, render descriptors and smoke coverage.
- Added architecture, render, gameplay, interaction, Agriculture-cutover and deploy audits.
- Refreshed required root `.agent` files and machine registry.
- Pushed only to `main`; created no branch or pull request.

## Main finding

The cutover improves domain ownership and introduces plan-based Agriculture commits, but recovery remains observationally incomplete. Snapshot reload cannot retract queued Agriculture events or ECS journal rows. The recovery shortcut can recreate the parent record from an Agriculture child record without validating the paired Inventory child record and resource delta. Legacy migration converts farming state but does not reconcile old transaction history with the new ledger family.

## Required authority

`cozy-island-agriculture-cutover-recovery-authority-domain`

## Validation

Documentation only. No runtime, test, package or deployment behavior changed. Executable failure and migration-reconciliation fixtures remain unimplemented.