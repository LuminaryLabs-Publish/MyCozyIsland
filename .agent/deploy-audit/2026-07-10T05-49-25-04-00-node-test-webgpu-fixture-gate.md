# Deploy Audit: Node Test + WebGPU Fixture Gate

**Repository:** `LuminaryLabs-Publish/MyCozyIsland`

**Timestamp:** `2026-07-10T05-49-25-04-00`

## Current package gate

```json
{
  "scripts": {
    "test": "node tests/static-check.mjs && node tests/domain-smoke.mjs"
  }
}
```

## Current checks

```txt
tests/static-check.mjs
  -> validates 50-kit catalog
  -> enforces unique kit IDs and DomainServiceKit base
  -> scans source for unseeded randomness / Date.now outside main-cloudform
  -> verifies WebGPU renderer source tokens
  -> verifies index.html imports Three.js 0.185.0 WebGPU and main-cloudform

tests/domain-smoke.mjs
  -> composes deterministic source snapshot
  -> compares repeated terrain/shoreline/vegetation/rock samples
  -> asserts foam/cloud/fog facts
  -> ticks scenario once
```

## Missing gate

```txt
No npm run check alias exists.
No DOM-free WebGPU consumer fixture exists.
No browser smoke validates CozyIslandHost.
No render-consumption ledger is generated by tests.
No fixture verifies route-token/readback drift.
No CI or Pages gate is recorded here.
```

## Next deploy gate

```txt
npm run fixture:webgpu-consumer
npm run check
npm test
```

`npm run check` should include the existing `npm test` plus the new source/consumer fixture. Browser/WebGPU smoke can remain separate because GPU availability may vary, but host readback should be fixture-safe.

## Validation this pass

Docs-only. Runtime source did not change and no commands were run.
