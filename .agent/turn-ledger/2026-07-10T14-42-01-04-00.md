# Turn Ledger: MyCozyIsland Frame Correlation Audit

Timestamp: 2026-07-10T14-42-01-04-00

## Goal

Refresh the repo-local architecture breakdown and identify the smallest proof-first implementation that can establish causal browser-host readback without altering the current WebGPU presentation.

## Completed

- Compared the full accessible Publish inventory against central ledger recency.
- Selected only `MyCozyIsland`; excluded `TheCavalryOfRome`.
- Re-read `src/main-cloudform.js`, package validation, and current `.agent` records.
- Reconfirmed the full interaction loop, domain inventory, services, and kit catalog.
- Refined the proof gap from generic readback to ordered frame correlation.
- Updated root docs and added timestamped system audits.
- Pushed documentation directly to `main`; no branch or PR.

## Evidence

`src/main-cloudform.js` composes all deterministic source rows, installs event consumers, ticks the scenario, copies camera state, updates renderers, samples performance, submits the post pipeline, and exposes only aggregate current state.

## Decision

The next implementation must introduce one bounded host journal with common record headers. Independent input, camera, performance, or render arrays are insufficient because they cannot prove causality or ordering.

## Validation

Documentation-only. Runtime tests, browser smoke, WebGPU validation, and the not-yet-created Node fixture were not run.