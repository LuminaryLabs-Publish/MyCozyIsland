# Turn Ledger: MyCozyIsland Dynamic Environment Frame Authority

Timestamp: `2026-07-12T03-39-52-04-00`

## Goal

Document one canonical environment-frame boundary without changing runtime behavior.

## Completed

```txt
compared all ten accessible LuminaryLabs-Publish repositories
excluded TheCavalryOfRome
confirmed all nine eligible repositories had central ledger and root .agent state
selected only MyCozyIsland as the oldest eligible synchronized repository
read environment, atmosphere, composition, scenario, host, renderer and test sources
identified mixed scenario clock, TSL global time and startup descriptor authorities
cataloged all active domains, 50 cataloged kits, one extra runtime kit, nine providers and five imported services
refreshed START_HERE, current audit, next steps, known gaps, validation and kit registry
added tracker, architecture, render, gameplay, interaction, environment and deploy audits
pushed documentation only to main
created no branch or pull request
```

## Main finding

```txt
world and shoreline foam use scenario elapsedSeconds
ocean, clouds and fog use Three TSL global time
illumination and wind-derived descriptors are frozen at startup
scenario reset restarts only the repository clock and camera
first reset frame can combine restarted and continuing environment phases
no environment frame, clock revision, reset generation, receipt or visible-frame proof exposes the mismatch
```

## Validation

```txt
runtime source changed: no
tests run: no
browser smoke run: no
new fixtures implemented: no
```