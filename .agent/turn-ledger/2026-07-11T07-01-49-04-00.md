# Turn Ledger: MyCozyIsland Runtime Session Lifecycle

Timestamp: `2026-07-11T07-01-49-04-00`

## Plan ledger

**Goal:** record the documentation-only pass that made route-session ownership, startup rollback, callback fencing, exact WebGPU disposal and restart the next implementation gate.

- [x] Selected only `LuminaryLabs-Publish/MyCozyIsland`.
- [x] Excluded `TheCavalryOfRome`.
- [x] Traced route construction, animation, focus, rendering and pagehide behavior.
- [x] Reconfirmed all domains, 50 local kits, imported services and runtime-implied hosts.
- [x] Added lifecycle-specific architecture, render, gameplay, interaction and deploy audits.
- [x] Updated required root `.agent` files.
- [x] Changed no runtime source.
- [x] Pushed only to `main`.

## Decision

The repository has a useful Core World wrapper and isolated renderer-disposal helper, but no session owner coordinates them with browser listeners, loader timers, the renderer animation loop, generated textures, post-processing, renderer/backend or `globalThis.CozyIsland`.

The first safe implementation ledge remains:

```txt
MyCozyIsland Runtime Session Lifecycle Authority
+ Startup Rollback / Stop / Dispose / Restart Fixture Gate
```

Pinned Core World contract parity remains second. Visible provider-cell rendering remains third.

## Validation

Documentation only. No tests or browser smoke were run, and no branch or pull request was created.
