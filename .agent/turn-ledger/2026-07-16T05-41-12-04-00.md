# Turn ledger: MyCozyIsland save compatibility

**Timestamp:** `2026-07-16T05-41-12-04-00`  
**Repository:** `LuminaryLabs-Publish/MyCozyIsland`  
**Branch:** `main`  
**Status:** `save-world-content-compatibility-admission-authority-audited`

## Summary

Selected MyCozyIsland as the oldest synchronized eligible Publish repository and documented the missing compatibility-admission boundary between portable saves, current world generation, content definitions, installed participants and the first restored visible frame.

## Plan ledger

**Goal:** record one bounded documentation run and preserve the implementation and proof boundary.

- [x] Compared the full Publish inventory with the central ledger.
- [x] Excluded TheCavalryOfRome.
- [x] Selected only MyCozyIsland.
- [x] Inspected save, world, Agriculture, Foraging, render-snapshot and smoke-test paths.
- [x] Identified all domains, kits, adapters and services.
- [x] Added the timestamped audit family.
- [x] Refreshed root `.agent` routing files and machine registry.
- [ ] Implement and execute compatibility admission and migration fixtures.

## Finding

Save v2 verifies checksum and schema but does not bind the envelope to a world-generation version, world-config digest, content catalog, dependency revision or installed participant schema. Restore mutates an already-created current runtime model; `cozyWorld.loadSnapshot()` forces the current configured ID and seed, while model-backed plots, forage nodes, surface queries and render base remain the current model.

## Authority

```txt
cozy-island-save-world-content-compatibility-admission-authority-domain
```

## Validation

Documentation only. No runtime, save, gameplay, renderer, package, test, workflow or deployment behavior changed.