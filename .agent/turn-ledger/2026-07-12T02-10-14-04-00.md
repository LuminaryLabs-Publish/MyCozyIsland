# Turn Ledger: MyCozyIsland Camera Rail Baseline Authority

Timestamp: `2026-07-12T02-10-14-04-00`

## Goal

Document the camera rail baseline and reset-fidelity boundary without changing runtime behavior.

## Completed

```txt
selected MyCozyIsland as the oldest fully synchronized eligible repo
excluded TheCavalryOfRome
read browser input, camera sequence, scenario, render projection and tests
identified rail drag mutation and incomplete reset
cataloged all active domains, kits, providers and imported services
refreshed START_HERE, current audit, next steps, known gaps and validation
advanced kit-registry schema to 72
added tracker, architecture, render, gameplay, interaction, camera and deploy audits
pushed documentation only to main
created no branch or pull request
```

## Main finding

```txt
input.drag mutates authored railPositions while in rail mode
cameraSequence.reset does not restore railPositions
scenario.reset delegates to that incomplete reset
post-reset camera progress can be baseline while path geometry is not
no baseline, path, reset, command or visible-frame revisions expose the mismatch
```

## Validation

```txt
runtime source changed: no
tests run: no
browser smoke run: no
new fixtures implemented: no
```