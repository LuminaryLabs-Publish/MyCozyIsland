# Turn Ledger: MyCozyIsland Browser Input Command Authority

Timestamp: `2026-07-12T06-51-27-04-00`

## Goal

Document one normalized, frame-admitted browser input command authority without changing runtime behavior.

## Completed

```txt
compared all ten accessible LuminaryLabs-Publish repositories
excluded TheCavalryOfRome
confirmed all nine eligible repositories had central ledger and root .agent coverage
selected only MyCozyIsland as the oldest eligible repository
read active browser listeners, camera sequence, scenario, public readback, and tests
identified raw wheel deltaY use without deltaMode normalization
identified pointer event-cadence divergence from per-event rail clamping
identified direct DOM-to-camera mutation outside frame admission
identified incomplete focus, visibility, and pointer-capture retirement
cataloged all active domains, 50 cataloged kits, one extra runtime kit, nine providers, and five imported services
refreshed START_HERE, current audit, next steps, known gaps, validation, and kit registry
added tracker, architecture, render, gameplay, interaction, input, and deploy audits
pushed documentation only to main
created no branch or pull request
```

## Main finding

```txt
wheel units are ambiguous
pointer segmentation can change rail geometry
keyboard holds are ambient mutable state
input has no command identity, sequence, frame target, or state revision
focus and capture loss have no typed clear result
no replay or visible-frame acknowledgement exists
```

## Required parent domain

```txt
cozy-island-browser-input-command-authority-domain
```

## Validation

```txt
runtime source changed: no
input behavior changed: no
camera behavior changed: no
render output changed: no
tests run: no
browser smoke run: no
new fixtures implemented: no
```