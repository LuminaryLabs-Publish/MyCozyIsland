# Turn ledger — MyCozyIsland host save durability

**Timestamp:** `2026-07-17T03-06-12-04-00`  
**Repository:** `LuminaryLabs-Publish/MyCozyIsland`  
**Branch:** `main`

## Goal

Document the full product interaction loop, domains, kits and services, then isolate the boundary between in-memory save capture and durable browser storage commitment.

## Selection result

- Full Publish inventory compared: 11 repositories.
- `LuminaryLabs-Publish/TheCavalryOfRome` excluded.
- Eligible repositories: 10.
- New, ledger-missing, root-agent-missing, undocumented or runtime-ahead repositories: 0.
- Selected only `LuminaryLabs-Publish/MyCozyIsland` through the oldest synchronized documented-selection rule.

## Source inspected

- `src/main-adventure.js`
- `src/adventure/persistence-render-domains.js`
- `src/adventure/composition-runtime.js`
- `tests/adventure-domains-smoke.mjs`
- `package.json`
- current root `.agent` records
- central MyCozyIsland ledger

## Finding

`cozySave.capture()` marks the domain as captured before `localStorage.setItem` succeeds. A storage exception is returned only from the host helper, while the domain remains captured and the HUD can still render `Saved`. Existing tests prove in-memory envelope behavior but not host persistence, storage failure or visible-status convergence.

## Output

Added the timestamped tracker, architecture, render, gameplay, interaction, save-system, deployment and central-sync audits. Refreshed `START_HERE.md`, `current-audit.md`, `next-steps.md`, `known-gaps.md`, `validation.md` and `kit-registry.json`.

## Change boundary

Documentation only. No branch or pull request was created. Runtime, save, rendering, gameplay, tests and deployment were unchanged.
