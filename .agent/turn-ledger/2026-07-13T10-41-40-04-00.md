# Turn ledger: MyCozyIsland Core Startup reconciliation

**Timestamp:** `2026-07-13T10-41-40-04-00`  
**Branch:** `main`

## Summary

Reconciled the recent Core Startup integration. MyCozyIsland now uses one engine for startup and gameplay, factual preparation state, continuation selection, structured failure projection, bounded initialization waits and a first-render gate. The remaining gap is that the complete browser module graph must load before the startup host exists.

## Plan ledger

**Goal:** document the implemented startup composition and the missing early browser-bootstrap admission boundary.

- [x] Compare the Publish repository inventory with central ledgers and root `.agent` state.
- [x] Exclude `TheCavalryOfRome`.
- [x] Select only MyCozyIsland because runtime was ten commits ahead of central documentation.
- [x] Read the HTML entry, startup host, composition runtime, browser loop, post pipeline, tests and package scripts.
- [x] Preserve the kit and service inventory and add Core Startup.
- [x] Add the timestamped audit family.
- [x] Refresh required root `.agent` documents.
- [ ] Add early module-load failure handling and browser/Pages fixtures.

## Findings

```txt
implemented:
  one Core Startup launch and one adventure engine
  six preparation facts
  continuation receipt
  structured pre-playable failures
  renderer and atmosphere timeouts
  first render call before playable entry
  headless startup smoke

missing:
  provider-independent early bootstrap owner
  bounded module-load result
  import-map and CDN failure result
  parse and evaluation failure result
  renderer-derived submission receipt
  first visible frame acknowledgement
  startup-host disposal on terminal retirement
```

## Validation

Documentation only. No runtime or deployment source changed. The startup smoke and package wiring were inspected but not independently executed.