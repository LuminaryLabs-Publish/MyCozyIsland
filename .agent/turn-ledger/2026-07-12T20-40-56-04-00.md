# Turn ledger: MyCozyIsland durable save commit authority

**Timestamp:** `2026-07-12T20-40-56-04-00`  
**Repository:** `LuminaryLabs-Publish/MyCozyIsland`  
**Branch:** `main`

## Summary

Selected MyCozyIsland after comparing all ten accessible Publish repositories and excluding TheCavalryOfRome. ZombieOrchard had a newer partial repo-local documentation pass that had not yet reached its root routing state, so this run avoided colliding with it and used the oldest stable synchronized fallback.

The audit traced browser startup restore, autosave, pagehide persistence, the portable save DSK, migration, sequential participant restore, rollback and HUD status projection. It found that an in-memory capture is published as `captured` before browser storage succeeds, allowing the HUD to say `Saved` after a failed `localStorage.setItem()`. It also found that restore reports `rolledBack: true` even if rollback itself throws.

## Plan ledger

**Goal:** document one truthful durable-save transaction from capture through browser storage, restore, rollback and visible status.

- [x] Compare full Publish inventory with central ledgers.
- [x] Verify root `.agent` coverage.
- [x] Select one repository only.
- [x] Read the composition runtime, save DSK, host persistence adapter, frame loop, HUD projection and smoke test.
- [x] Preserve the complete 64-kit/service census.
- [x] Define the parent authority and required fixtures.
- [x] Add tracker and audit folders.
- [x] Refresh required root documents.
- [x] Synchronize the central ledger and change log.
- [x] Push directly to `main` and create no branch or pull request.
- [ ] Implement and execute the save durability fixture matrix.

## Evidence

```txt
src/adventure/composition-runtime.js
src/adventure/persistence-render-domains.js
src/main-adventure.js
tests/adventure-domains-smoke.mjs
package.json
```

## Main result

```txt
cozySave.capture()
  -> SaveState.status = captured
  -> saveCount and revision increment

browser adapter
  -> localStorage.setItem()
  -> may throw quota/security/storage error

HUD
  -> frame.save.status === captured
  -> displays Saved even when adapter returned ok:false
```

Rollback result mismatch:

```txt
participant restore fails
  -> rollback attempt also fails
  -> rollback error is logged
  -> restore still returns rolledBack:true
```

## Non-claims

No runtime, storage, save schema, rendering, dependency, package, CI or deployment behavior changed. No durable-write, atomic-restore, truthful-rollback or visible-save-status correctness claim is made.