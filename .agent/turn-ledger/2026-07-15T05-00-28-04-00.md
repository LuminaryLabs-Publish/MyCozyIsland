# Turn ledger: MyCozyIsland host-clock fixed-step simulation

**Timestamp:** `2026-07-15T05-00-28-04-00`  
**Status:** `host-clock-fixed-step-simulation-authority-audited`

## Summary

Selected MyCozyIsland through the oldest synchronized eligible-repository rule. The audit is documentation-only and records that RAF elapsed time is clipped to one 50 ms simulation step per callback, making scenario time, movement, stamina, Agriculture growth, Foraging respawn and autosave cadence slow below 20 FPS.

## Plan ledger

**Goal:** record the selection, source inspection, complete DSK inventory, clock finding, authority boundary and validation limits for this run.

- [x] Compared 11 Publish repositories with ten central ledgers.
- [x] Excluded TheCavalryOfRome.
- [x] Confirmed all ten eligible root `.agent` states.
- [x] Confirmed each eligible current head matched its documented head.
- [x] Selected only MyCozyIsland, last centrally updated at `2026-07-15T01-04-57-04-00`.
- [x] Inspected `src/main-adventure.js`, `src/adventure/composition-runtime.js`, `src/adventure/runtime-domains.js`, `src/adventure/resource-domains.js`, `game.html`, tests and package scripts.
- [x] Preserved all 70 documented kit/adapter surfaces.
- [x] Added host-clock, architecture, render, gameplay, interaction, deploy and central-sync audits.
- [x] Refreshed required root `.agent` documents.
- [ ] Runtime implementation and executable fixtures remain future work.

## Finding

```txt
RAF gap -> max 100 ms
simulation delta -> max 50 ms
composition delta -> max 50 ms
one callback -> one engine tick
no accumulator -> excess elapsed time discarded
```

At 10 FPS, one wall second produces about 0.5 simulation seconds. No typed result reports admitted time, executed steps, residual time or discarded time.

## Authority

```txt
cozy-island-host-clock-fixed-step-simulation-authority-domain
```

It must admit monotonic elapsed time, classify suspension and overload, execute bounded deterministic fixed steps, publish clock receipts, bind every time consumer and acknowledge the first rendered frame matching the accepted simulation revision.

## Validation

No runtime, tests, dependencies, workflows or deployment behavior changed. No fixed-step or production-readiness claim is made.