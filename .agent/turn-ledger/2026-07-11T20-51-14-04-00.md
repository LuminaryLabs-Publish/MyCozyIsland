# Turn Ledger: MyCozyIsland World Lifecycle Contract

Timestamp: `2026-07-11T20-51-14-04-00`

## Summary

Audited the public world-runtime lifecycle surface shared by legacy and Core modes. The same methods currently imply incompatible reset/dispose semantics, expose no lifecycle phase or generation, and leave use-after-dispose behavior undefined.

## Plan ledger

**Goal:** preserve the source-backed findings and implementation boundary for a mode-consistent, generation-fenced world lifecycle.

- [x] Compared Publish inventory and central ledger.
- [x] Excluded `TheCavalryOfRome`.
- [x] Skipped active `PrehistoricRush` runtime work.
- [x] Selected `MyCozyIsland` only.
- [x] Read route, world wrapper, current audits and test chain.
- [x] Catalogued domains, kits, providers and services.
- [x] Added lifecycle parity architecture and system audits.
- [x] Refreshed required root `.agent` documents.
- [x] Changed no runtime source.
- [ ] Runtime authority and fixtures remain unimplemented.

## Source facts

```txt
legacy prepare/reset/dispose: reversible prepared flag
core reset: resetWorlds + materializer reset
core dispose: reset + Core World domain reset
closed/disposed flag: absent
world generation: absent
typed lifecycle results: absent
query lease revocation: absent
post-dispose fixture: absent
legacy/core parity fixture: absent
```

## Decision

Add `cozy-island-world-lifecycle-contract-authority-domain` ahead of focus, materialization and render-commit work. It must distinguish reusable reset from terminal disposal and return the same typed result schema in both world modes.
