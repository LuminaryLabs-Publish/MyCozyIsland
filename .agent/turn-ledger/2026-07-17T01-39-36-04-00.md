# Turn ledger — 2026-07-17T01-39-36-04-00

**Repository:** `LuminaryLabs-Publish/MyCozyIsland`  
**Branch:** `main`  
**Selection:** runtime-ahead priority  
**Reviewed runtime revision:** `347c78f358994822f9fedf91c3e16d33d6909e7e`

## Completed

- Compared the full 11-repository `LuminaryLabs-Publish` inventory against the central Publish ledger set.
- Excluded `LuminaryLabs-Publish/TheCavalryOfRome`.
- Confirmed ten eligible central ledger entries and root `.agent` states.
- Selected only MyCozyIsland because it was six runtime commits ahead of its documented repo-local head.
- Inspected the six-file particle-first menu performance delta.
- Reconciled the interaction loop, domains, all kits and all offered services.
- Counted 85 implemented kit/adapter surfaces.
- Defined 18 proposed preload-to-ready handoff surfaces.
- Added the timestamped tracker and focused audit family.
- Refreshed the required root `.agent` indexes and machine registry.

## Main finding

The runtime now switches from a preload presentation policy to a ready presentation policy, but `markReady()` enables Play immediately after `setPreloading(false)` requests the DPR and frame-mode transition. No matching ready-frame acknowledgement exists before the player can enter.

## Change boundary

```txt
documentation changed: yes
runtime changed by this turn: no
tests or workflows changed: no
deployment changed: no
branch created: no
pull request created: no
```

## Validation not performed

```txt
npm test
browser render
DPR/frame-mode handoff fixture
rapid Play activation fixture
stale or duplicate ready-message fixture
built artifact smoke
Pages-origin smoke
```

No runtime handoff fix or production-readiness claim is made.